- clean: ['~']

- link:
    ~/.profile: profile
    ~/.bashrc: bashrc
    ~/.bash_logout: bash_logout
    ~/.gitconfig: gitconfig
    ~/.zshrc: zshrc

- crossplatform-shell:
  -  description: Install Mac based apps
     stdout: true
     platform: darwin
     command: |
      echo "Installing Mac.  Blerg"

